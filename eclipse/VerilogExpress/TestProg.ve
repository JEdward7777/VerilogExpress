module testProg{
	reg x = 0;
	reg y = 0;
	reg z = 0;
	

	while( x < 10 ){
		y = 0;
		while( y < 10 ){
			y = y + 1;
			if( x==y ){
				z = z + 1;
			}
		}
		
		x = x + 1;
	}	
	
	output xOut;
	output yOut;

	xOut = x;
	yOut = y;
	stdout = 1;
}